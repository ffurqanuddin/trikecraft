import 'package:flutter/material.dart';

class NewOrdersTab extends StatelessWidget {
  const NewOrdersTab({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("No new bike orders yet."));
  }
}
